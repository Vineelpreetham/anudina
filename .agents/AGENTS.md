# Git Deployment Rules

When committing and pushing code for this project, always push to the `myfork` remote, because that is the repository connected to Vercel for production deployments.

**Command:**
```bash
git push myfork main
```
